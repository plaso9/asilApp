<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.uniba.di.sms.asilapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MedicalRecordsActivity"></activity>
        <activity android:name=".PersonalDataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InformativeActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".AcceptanceActivity" />
        <activity android:name=".CityInfoActivity" />
        <activity android:name=".BylawActivity" />
        <activity android:name=".UsefulNumbersActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".QuestionnairesActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomepageActivity" />
    </application>

</manifest>